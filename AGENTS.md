# Agent Notes

- This project uses Next.js 16 with the App Router. Before changing Next.js
  routes, config, or rendering behavior, check the relevant guide in
  `node_modules/next/dist/docs/`.
- Keep `.env`, `.vercel`, build output, and local scratch files private.
- Run `npm run release:check` before deployment-oriented changes.
